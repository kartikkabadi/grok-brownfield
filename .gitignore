# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Local / temp artifacts (never commit run outputs)
# Note: /tmp/ ignores a top-level tmp/ directory in this repo (not system /tmp).
/tmp/
grok-brownfield-state-*.json
grok-brownfield-*.md

# Env / secrets
.env
.env.*
*.pem
*.key

# Test fixtures created outside mktemp in local runs
fixture-home/
broken-home/